---
"@platforma-open/milaboratories.mixcr-clonotyping-2": minor
---

Adopt the slim facade: the block is published via the `block/` package with a from-pack-v2 BlockPointer and zero `@platforma-sdk/*` runtime dependencies. Tests consume `samples-and-data` through its published facade only.
